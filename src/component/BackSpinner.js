import React from "react";
import Spinner from "./Spinner";
const BackSpinner = () => {
  return (
    <div className="backspinner_container">
      <Spinner />
    </div>
  );
};

export default BackSpinner;
